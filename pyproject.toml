[project]
name = "xtb-lab-harness"
version = "0.3.0"
description = "MCP server for xTB-based molecular calculation and LLM multi-agent experiment design."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Edmond Ko" }]
dependencies = [
    "mcp>=1.9.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
]
llm = [
    "google-genai>=1.0",
]

[project.scripts]
xtb-lab-harness = "xtb_lab_harness.server:main"
xtb-lab-run = "xtb_lab_harness.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/xtb_lab_harness"]

[tool.hatch.build.targets.wheel.sources]
"src/xtb_lab_harness" = "xtb_lab_harness"

[tool.hatch.build.targets.sdist]
include = ["src/xtb_lab_harness", "examples", "docs"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
